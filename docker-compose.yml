services:
  mongodb:
    image: mongo:7
    container_name: pur_beurre_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: pur_beurre

volumes:
  mongodb_data:
