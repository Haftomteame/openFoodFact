{
  "name": "pur-beurre-ratatouille",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "backend\\.venv\\Scripts\\python backend\\manage.py runserver 8080",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend && backend\\.venv\\Scripts\\pip install -r backend\\requirements.txt",
    "seed": "backend\\.venv\\Scripts\\python backend\\scripts\\seed_data.py"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
